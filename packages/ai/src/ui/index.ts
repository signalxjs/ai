/**
 * @sigx/ai/ui — `@sigx/json-ui` for the model: `uiTool()`, a `defineTool`
 * whose description is the catalog. Peers on `@sigx/json-ui` (optional: the
 * rest of `@sigx/ai` needs no UI package).
 */

export type { UIToolInput, UIToolResult, UIToolOptions } from './ui-tool.js';
export { uiTool, uiToolJsonSchema, uiToolInputSchema } from './ui-tool.js';
